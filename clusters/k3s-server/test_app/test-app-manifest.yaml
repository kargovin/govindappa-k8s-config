# test-app-manifest.yaml (in your Config Repo)

# 1. Deployment (The application container)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-app-deployment
  namespace: default
  labels:
    app: gitops-test-app # Optional: Label the deployment itself
spec:
  replicas: 1
  # 🛑 FIX: The Selector block links the Deployment to its Pods
  selector:
    matchLabels:
      app: gitops-test-app # <--- MUST MATCH the Pod's labels below
  template:
    metadata:
      labels:
        app: gitops-test-app # <--- The Pod's label
    spec:
      containers:
      - name: test-app-container
        # CRITICAL: Flux will update this tag automatically!
        image: k4rth/gitops-test-app:main-1773145001-87d8c146f3cfbc0d0fc8e66d73b80717f4775516 # {"$imagepolicy": "flux-system:gitops-test-app-policy"}
        ports:
        - containerPort: 8080 # Matches the Dockerfile CMD port
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          periodSeconds: 30

        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          periodSeconds: 10
---
# 2. Service (Internal Load Balancing)
apiVersion: v1
kind: Service
metadata:
  name: test-app-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: gitops-test-app # <--- MUST MATCH the Pod's labels
  ports:
  - port: 80
    targetPort: 8080 # Matches the containerPort
---
# 3. Ingress (External Routing and SSL)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-app-ingress
  namespace: default
  annotations:
    external-dns.alpha.kubernetes.io/hostname: test.govindappa.com
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  rules:
  - host: test.govindappa.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: test-app-service
            port:
              number: 80
  tls:
  - hosts:
    - test.govindappa.com
    secretName: test-app-tls-cert
